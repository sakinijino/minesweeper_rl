# 优化方向 Backlog

## 当前状态

**5×5×3 基准**（EXP-018，2M 步，from scratch）：
- eval **86%**（500 局），EV=0.84，value_loss=0.242
- reward 配置：win=0.1, lose=-0.1, reveal=0.1（对称小幅，EV 最稳定）
- 这是后续所有大棋盘实验的推荐起点

**核心已知结论**：
- win/lose=±1.0 导致 EV 卡死（~0.43），缩小至 ±0.1 量级恢复正常（0.84）
- 8×8×10 在全部既往实验中均 0%，根因是终止信号方差过大+极稀疏 win signal
- 课程学习路径本身不是瓶颈，奖励尺度才是——从未用正确 reward 配置跑过完整课程

---

## 方向 A：攻克大棋盘（核心目标）

目标：在 8×8×10（标准初级扫雷）上达到有意义的胜率（>10%）。

**已排除（win/lose=±1.0 下所有变体）**：
- from scratch / Conv 直接迁移 / 分阶段课程（5→6→7→8）：均 0%
- 进度奖励（踩雷 bonus / 揭格乘数）：均导致 reward hacking，0%
- LR cosine 衰减：无效，稀疏奖励瓶颈与 LR 无关

**EXP-018 之后的核心方向**：

- [ ] **A6. 用 EXP-018 reward 重跑完整课程学习**：所有课程实验均使用 win/lose=±1.0，从未用小幅奖励试过。用 EXP-018（win=0.1/lose=-0.1）配置重跑 5×5→6×6→7×7→8×8，验证 EV=0.84 能否让 policy 在大棋盘持续学习。
  - 起点：EXP-018 checkpoint（5×5×3，EV=0.84）
  - Stage 6×6×5：目标 75%+（EXP-015a 用±1.0 已达 65%，正确奖励应更高）
  - Stage 7×7×7：目标 40%+（EXP-015b 用±1.0 仅 16%，预计明显改善）
  - Stage 8×8×10：观察 success_rate 是否出现上升趋势（突破 2% 即有价值）
  - **优先级最高**：是目前最自然的下一步，直接结合已知有效的两个要素

- [ ] **A7. 内在动机探索奖励（curiosity / RND）**：8×8×10 的 win signal 极稀疏（随机探索赢率 <1%），per-step reveal 奖励密度不足且引发 hacking。RND（Random Network Distillation）或 ICM（Intrinsic Curiosity Module）提供基于状态新颖性的密集信号，不依赖任务奖励，从根源解决探索问题。
  - 实现代价较高：需自定义 env wrapper 或 callback
  - 可与课程学习结合：在稀疏阶段（7×7→8×8）开启 intrinsic reward

- [ ] **A8. 纯稀疏奖励（去掉 reveal=0.1）**：当前 reveal=0.1 是主要 reward hacking 的来源（每步揭格即得奖励，赢不赢无关）。只保留 win/lose 终止信号，彻底消除中间奖励的 hacking 面，policy 只能从最终结果学习。
  - 风险：信号更稀疏，探索更难；但 hacking 不可能发生
  - 与 A7 搭配：intrinsic reward 填补密度，win/lose 提供目标

---

## 方向 B：提升训练效率

- [ ] **B1. 最优 checkpoint 自动选择**：EXP-007 的 1.75M 优于 2M final（87% vs 86%），训练完取 final 会损失 1-2%。在训练时加入 eval callback，每隔若干步评估并自动保存最优 checkpoint，对大棋盘长训练尤其重要（避免浪费后期偏移）。

- [ ] **B2. entropy 衰减**（ent_coef 从 0.01 cosine 降到 0.001）：EXP-017 的 entropy 过快收敛（-0.60），EXP-018 停在 -0.70 为正常水平。主动在 late-training 降低 ent_coef，可以在 plateau 后收紧策略、减少无效探索，可能提升 1-2% eval。

- [ ] **B3. 增加并行环境数**：当前 n_envs=16，大棋盘 episode 更长（平均步数 ~3×），提升至 32 或 64 可加速有效样本收集。需评估 T4 GPU 内存和 CPU subproc 瓶颈。

---

## 方向 C：模型结构（低优先级）

EXP-008 已排除容量假设（features_dim 256 无收益），以下改进独立于容量问题：

- [ ] **C1. 分离 pi/vf 特征提取器**：当前 policy 和 value function 共享 CNN，策略梯度更新干扰 value 拟合（尤其在大棋盘稀疏场景下，EV 爬升缓慢可能部分源于此）。分离后 value head 可独立优化。
- [ ] **C2. 更深 pi/vf 全连接层**（[128,128]）：当前 [64,64]，对 8×8×10 的复杂局面决策可能不足。与 A6 结合验证。

---

## 已完成实验汇总

| 实验 | 棋盘 | 核心变量 | eval | 结论 |
|------|------|---------|------|------|
| EXP-001 | 5×5×3 | 基线 100k 步 | 18% | 步数严重不足，流程验证 |
| EXP-002 | 5×5×3 | 扩到 800k 步 | 52% | 步数是早期主要瓶颈 |
| EXP-003 | 5×5×3 | 2M 步（win=0.2/lose=-0.05） | 83% | EV=0.84，小幅奖励下的自然上限 |
| EXP-004 | 5×5×3 | reward_win 0.2→1.0 @1M | 75% | 大幅奖励提升早期效率，但 EV 下降 |
| EXP-005 | 5×5×3 | 续训 EXP-004 到 2M | 84% | EV 卡 0.44，±1.0 瓶颈首次出现 |
| EXP-006 | 5×5×3 | vf_coef 0.5→1.0 | 80% | vf_coef 非瓶颈，单独用反而 -4% |
| EXP-007 | 5×5×3 | obs_channels 1→2 | **87%** | 双通道最优配置，早期收敛更快 |
| EXP-008 | 5×5×3 | features_dim 128→256 | 86% | 容量非瓶颈，+29% 算力无收益 |
| EXP-009 | 8×8×10 | from scratch 5M | **0%** | 稀疏奖励瓶颈，success_rate 全程 0% |
| EXP-010 | 8×8×10 | 5×5×3 Conv 直接迁移 | **0%** | 权重迁移无效，与 from scratch 无差异 |
| EXP-011a | 6×6×5 | 课程 Stage2（from 5×5×3） | 38% | 中间棋盘可学，但未 plateau |
| EXP-011b | 8×8×10 | 课程 Stage3（from 6×6×5） | **1%** | 象征性突破，±1.0 奖励下的上限 |
| EXP-012 | 8×8×10 | 进度奖励（踩雷时 bonus） | **0%** | reward hacking：模型学会快速死 |
| EXP-013 | 8×8×10 | 进度奖励（揭格乘数） | **0%** | reward hacking，3.4M 提前终止 |
| EXP-014 | 8×8×10 | LR cosine 衰减（0.001→0.0001） | **0%** | 稀疏奖励瓶颈，LR 无关 |
| EXP-015a | 6×6×5 | 渐进课程 Stage2（5M 步） | **65%** | 充分收敛，达成目标 ✓ |
| EXP-015b | 7×7×7 | 渐进课程 Stage3（from 015a） | **16%** | 有学习，未达 40% 目标 |
| EXP-015c | 8×8×10 | 渐进课程 Stage4（from 015b） | **0%** | ±1.0 下课程路径穷尽，确认奖励是根因 |
| EXP-016 | 5×5×3 | win=0.2/lose=-0.05，2M | **86%** | EV=0.84 ✅，三组统计等价（500局） |
| EXP-017 | 5×5×3 | win=0.05/lose=-0.2，2M | **85%** | EV=0.83 ✅，后期 value_loss 回弹，不稳定 |
| EXP-018 | 5×5×3 | win=0.1/lose=-0.1，2M | **85.6%** | EV=0.84 ✅，最稳定，**后续实验推荐基准** |
